if (head == NULL)
    display 'List is em[ty !! Deletion is not psooible'
else 
    define a Node pointer 'temp' and initalize with head;
        head = temp -> link 
        delete temp 
        count = count -1